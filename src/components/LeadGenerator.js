import React from 'react';

const LeadGenerator = () => (
  <section>
    <h1>AI_agent for Lead Generation</h1>
    <p>Lead classification and Outbound message generation</p>
  </section>
);

export default LeadGenerator;